# Base URL API backend
# Lokal (default, tidak perlu diisi karena sudah ada fallback):
# VITE_API_URL=http://localhost:5000/api
#
# Contoh saat deploy di Vercel/Netlify (backend di Render):
# VITE_API_URL=https://backend-kamu.onrender.com/api
